<form method="post" action="{{ path('galerie_delete', {'id': galeryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ galeryId) }}">
    <button class="btn">Delete galery</button>
</form>
